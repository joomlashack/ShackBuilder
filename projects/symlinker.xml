<?xml version="1.0" encoding="UTF-8"?>
<project name="Symlinker" default="">

    <taskdef name="createsymlinks" classname="CreateSymlinksTask" />
    <taskdef name="unlinksynlinks" classname="UnlinkSymlinksTask" />

    <property name="symlink.map.path" value="${phing.dir}/../symlinks.yml"/>

    <!-- Create symlinks for the extension in development environments -->
    <target name="symlink">
        <input propertyName="basepath" message="Joomla root path: "/>
        <createsymlinks mapfile="${symlink.map.path}" basepath="${basepath}" phingdir="${phing.dir}"/>
    </target>

    <!-- Remove symlinks for the extension in development environments -->
    <target name="unlink">
        <input propertyName="basepath" message="Joomla root path: "/>
        <unlinksynlinks mapfile="${symlink.map.path}" basepath="${basepath}" phingdir="${phing.dir}"/>
    </target>

</project>
